# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Item {
  itemID: ID!
  itemData: String!
}

type Drawing {
  drawingID: ID!
  items: [Item!]!
}

type Segment {
  userID: ID!
  drawingID: ID!
  itemID: ID!
  segmentData: String!
  strokeColor: [Float!]
  fillColor: [Float!]
  strokeWidth: [Int!]
}

input NewSegmentInput {
  userID: ID!
  drawingID: ID!
  itemID: ID!
  segmentData: String!
  strokeColor: [Float!]
  fillColor: [Float!]
  strokeWidth: [Int!]
}

type Query {
  drawing(userID: ID!, drawingID: ID!): Drawing!
}

type Mutation {
  createDrawing(createDrawingData: CreateDrawingInput!): Drawing!
  addItem(addItemData: AddItemInput!): Item!
  deleteItem(userID: ID!, drawingID: ID!, itemID: ID!): Item!
  publishNewSegment(newSegmentData: NewSegmentInput!): Segment!
}

input CreateDrawingInput {
  userID: ID!
  drawingID: String!
}

input AddItemInput {
  userID: ID!
  drawingID: ID!
  itemID: ID!
  itemData: String!
}

type Subscription {
  itemAdded(userID: ID!, drawingID: ID!): Item!
  itemRemoved(userID: ID!, drawingID: ID!): Item!
  newSegmentPublished(userID: ID!, drawingID: ID!): Segment!
}
